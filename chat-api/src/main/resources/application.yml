server:
  port: 8042

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/vue_chat
    driver-class-name: org.mariadb.jdbc.Driver
    username: danny
    password: danny!@#123
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MariaDB106Dialect
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create-drop